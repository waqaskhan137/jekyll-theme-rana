---
title: "Typography sample"
category: demo
tag: [demo, typography]
---

Body text sets in Charter, with DM Sans for interface elements and Fira Code for code. This
post exists so you can judge the reading experience on real structures.

## A second-level heading

A paragraph with **bold**, *italic*, a [link](https://github.com/waqaskhan137/jekyll-theme-rana),
and `inline code`. Lists:

- The first item
- The second item, slightly longer so the line height is visible
- The third item

> A blockquote, for the moments a post needs someone else's words.

### Code

```python
def feeling_of_knowing(cues):
    """Confidence is an inference, not a readout."""
    return sum(cues) / len(cues)
```

### A table

| Layer    | Typeface  | Used for          |
| -------- | --------- | ----------------- |
| Prose    | Charter   | Posts and pages   |
| UI       | DM Sans   | Chrome and labels |
| Code     | Fira Code | Snippets          |
